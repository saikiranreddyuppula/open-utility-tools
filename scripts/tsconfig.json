{
  "comment": "Bun runtime scripts (registry codegen). Typechecked separately from the Next app since they use Bun-only globals (Bun, the 'bun' module, import.meta.dir).",
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "types": ["bun"],
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "paths": { "@/*": ["../*"] }
  },
  "include": ["gen-registry.ts", "selftest.ts"],
  "comment2": "gen-tools-doc.ts imports the app registry (which imports .tsx); it is validated by Bun at runtime and type-checked via the app's tsc, not here."
}
