---
"@open-utility-tools/core": minor
---

Add per-tool deep subpath exports and the first WASM-backed, isomorphic raster image tools (`./image/convert`, `./image/probe`) that run identically in the browser, Node, and Bun. Adds the extracted `./time/add-business-days` tool. Wires cross-runtime E2E tests (Node + Bun) and TypeDoc API docs.
