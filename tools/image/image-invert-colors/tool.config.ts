import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'image-image-invert-colors-v1',
  name: 'Invert Colors (Negative)',
  slug: 'image-invert-colors',
  description: "Create a photographic negative by inverting every pixel's RGB values.",
  category: 'image',
  tags: ['invert', 'negative', 'colors', 'filter', 'pixels'],
  keywords: [
    'invert colors',
    'negative image',
    'photo negative',
    'invert luminance',
    'film negative',
    'color invert',
  ],
  icon: 'Contrast',
  relatedTools: [],
};

export default meta;
