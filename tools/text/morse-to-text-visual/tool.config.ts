import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'text-morse-to-text-visual-v1',
  name: 'Morse Code Visual Decoder',
  slug: 'morse-to-text-visual',
  description: 'Decode Morse with timing/visual rendering and encode text with adjustable dot, dash, and gap symbols.',
  category: 'text',
  tags: ['morse', 'cipher', 'decode', 'encode', 'visual'],
  keywords: ['morse code', 'morse decoder', 'morse encoder', 'dot dash', 'itu morse', 'tap rhythm', 'sos'],
  icon: 'Radio',
  relatedTools: [],
};

export default meta;
