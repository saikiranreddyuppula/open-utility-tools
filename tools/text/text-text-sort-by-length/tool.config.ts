import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'text-text-text-sort-by-length-v1',
  name: 'Sort Lines by Length',
  slug: 'text-text-sort-by-length',
  description: 'Reorder lines by character length, ascending or descending, with tie-breaking.',
  category: 'text',
  tags: ['sort', 'lines', 'length', 'order', 'text'],
  keywords: ['sort by length', 'longest lines', 'shortest lines', 'reorder', 'arrange lines'],
  icon: 'SortAsc',
  relatedTools: [],
};

export default meta;
