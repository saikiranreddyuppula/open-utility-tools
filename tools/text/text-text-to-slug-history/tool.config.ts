import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'text-text-text-to-slug-history-v1',
  name: 'Slug Collision Disambiguator',
  slug: 'text-text-to-slug-history',
  description: 'Generate sequential unique slugs from a list, appending numeric suffixes on collision.',
  category: 'text',
  tags: ['slug', 'unique', 'collision', 'url', 'cms'],
  keywords: ['unique slug', 'slug suffix', 'permalink', 'duplicate slug', 'slugify list'],
  icon: 'Tags',
  relatedTools: [],
};

export default meta;
