import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'text-text-align-pad-v1',
  name: 'Text Alignment & Padding',
  slug: 'text-align-pad',
  description:
    'Pad or align each line to a fixed width, left, right, or centered, with a chosen fill character for tidy fixed-width columns.',
  category: 'text',
  tags: ['align', 'pad', 'format'],
  keywords: ['align', 'pad', 'center', 'right align', 'fixed width'],
  icon: 'AlignJustify',
  relatedTools: [],
};

export default meta;
