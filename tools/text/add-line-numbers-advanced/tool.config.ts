import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'text-add-line-numbers-advanced-v1',
  name: 'Line Numbering (Advanced)',
  slug: 'add-line-numbers-advanced',
  description:
    'Prefix lines with numbers using custom start, step, zero-padding, format template, and blank-line handling.',
  category: 'text',
  tags: ['line numbers', 'numbering', 'format', 'template', 'prefix'],
  keywords: ['number lines', 'zero pad', 'cat -b', 'line counter', 'numbered list', 'step start'],
  icon: 'ListOrdered',
  relatedTools: [],
};

export default meta;
