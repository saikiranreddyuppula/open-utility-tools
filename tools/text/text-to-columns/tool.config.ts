import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'text-text-to-columns-v1',
  name: 'Wrap Text Into Columns',
  slug: 'text-to-columns',
  description: 'Reflow a single stream of lines into multiple side-by-side newspaper-style columns.',
  category: 'text',
  tags: ['columns', 'layout', 'wrap', 'monospace', 'reflow'],
  keywords: ['multi column', 'newspaper layout', 'column wrap', 'side by side', 'grid text'],
  icon: 'Rows3',
  relatedTools: [],
};

export default meta;
