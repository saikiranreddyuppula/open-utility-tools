import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'encoding-encoding-base64url-v1',
  name: 'Base64 URL-Safe Encode / Decode',
  slug: 'encoding-base64url',
  description:
    'Encode or decode text using the URL-safe Base64 alphabet with optional padding for tokens and URLs.',
  category: 'encoding',
  tags: ['base64url', 'url safe', 'base64'],
  keywords: ['base64url', 'url safe', 'base64', 'encode', 'decode', 'token'],
  icon: 'Link2',
  relatedTools: [],
};

export default meta;
