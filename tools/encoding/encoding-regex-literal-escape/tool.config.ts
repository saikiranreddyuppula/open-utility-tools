import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'encoding-encoding-regex-literal-escape-v1',
  name: 'Regex Special-Character Escaper',
  slug: 'encoding-regex-literal-escape',
  description: 'Escape text so it matches literally inside a regular expression, for several flavors.',
  category: 'encoding',
  tags: ['regex', 'escape', 'pattern', 'literal', 'developer'],
  keywords: ['regex', 'escape', 'metacharacter', 'literal', 'quotemeta', 're.escape', 'pcre', 'character class'],
  icon: 'Regex',
  relatedTools: [],
};

export default meta;
