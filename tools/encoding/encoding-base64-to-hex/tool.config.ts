import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'encoding-encoding-base64-to-hex-v1',
  name: 'Base64 to Hex Converter',
  slug: 'encoding-base64-to-hex',
  description: 'Convert between Base64-encoded data and its hexadecimal byte representation in both directions.',
  category: 'encoding',
  tags: ['base64', 'hex', 'convert'],
  keywords: ['base64', 'hex', 'hexadecimal', 'convert', 'bytes'],
  icon: 'ArrowLeftRight',
  relatedTools: [],
};

export default meta;
