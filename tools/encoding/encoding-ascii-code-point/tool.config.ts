import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'encoding-encoding-ascii-code-point-v1',
  name: 'ASCII / Code Point Converter',
  slug: 'encoding-ascii-code-point',
  description:
    'Convert text to ASCII/Unicode code point numbers and convert numeric lists back to text.',
  category: 'encoding',
  tags: ['ascii', 'code point', 'convert'],
  keywords: ['ascii', 'code point', 'char code', 'convert', 'decimal', 'unicode'],
  icon: 'FileDigit',
  relatedTools: [],
};

export default meta;
