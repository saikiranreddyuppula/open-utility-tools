import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'time-time-zone-meeting-planner-v1',
  name: 'Fixed-Offset Meeting Planner',
  slug: 'time-zone-meeting-planner',
  description: 'See one meeting time across multiple fixed UTC offsets to find an overlap.',
  category: 'time',
  tags: ['meeting', 'timezone', 'utc offset', 'planner', 'overlap'],
  keywords: [
    'meeting planner',
    'time zone overlap',
    'utc offset',
    'world clock',
    'schedule across zones',
    'working hours',
  ],
  icon: 'Users',
  relatedTools: [],
};

export default meta;
