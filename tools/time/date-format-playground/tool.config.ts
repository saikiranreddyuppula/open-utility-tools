import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'time-date-format-playground-v1',
  name: 'Unicode Date Pattern Playground',
  slug: 'date-format-playground',
  description: 'Render a datetime using an Intl.DateTimeFormat-style or LDML pattern and preview the result.',
  category: 'time',
  tags: ['date', 'format', 'intl', 'ldml', 'locale'],
  keywords: ['date format', 'intl datetimeformat', 'ldml pattern', 'locale date', 'date pattern preview', 'format tokens'],
  icon: 'CalendarClock',
  relatedTools: [],
};

export default meta;
