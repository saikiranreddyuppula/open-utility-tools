import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'time-age-calculator-v1',
  name: 'Age Calculator',
  slug: 'age-calculator',
  description:
    'Calculate exact age from a birth date in years, months, days, plus total days, hours, and the next birthday countdown.',
  category: 'time',
  tags: ['age', 'date', 'birthday'],
  keywords: ['age', 'birthday', 'years old', 'calculate', 'date of birth'],
  icon: 'CalendarClock',
  relatedTools: [],
};

export default meta;
