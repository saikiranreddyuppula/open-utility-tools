import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'time-weeks-until-date-v1',
  name: 'Weeks & Months Until Date',
  slug: 'weeks-until-date',
  description: 'Count full weeks, months, and weekends remaining until a future date.',
  category: 'time',
  tags: ['weeks', 'months', 'countdown', 'weekends', 'date'],
  keywords: ['weeks until', 'months until', 'business days', 'weekends remaining', 'date countdown'],
  icon: 'Milestone',
  relatedTools: [],
};

export default meta;
