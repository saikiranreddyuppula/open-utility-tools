import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'time-date-add-subtract-v1',
  name: 'Add or Subtract Days',
  slug: 'date-add-subtract',
  description:
    'Add or subtract days, weeks, months, or years from a date and get the resulting date in multiple formats.',
  category: 'time',
  tags: ['date', 'math', 'offset'],
  keywords: ['date math', 'add days', 'subtract', 'calculator', 'offset'],
  icon: 'CalendarClock',
  relatedTools: [],
};

export default meta;
