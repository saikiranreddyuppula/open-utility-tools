import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'time-days-between-units-v1',
  name: 'Days Between in Many Units',
  slug: 'days-between-units',
  description: 'Measure the span between two datetimes simultaneously in years, months, weeks, days, hours, minutes, and seconds.',
  category: 'time',
  tags: ['date', 'difference', 'duration', 'units', 'between'],
  keywords: ['days between', 'time between dates', 'date difference units', 'weeks between', 'hours between', 'business days'],
  icon: 'ArrowLeftRight',
  relatedTools: [],
};

export default meta;
