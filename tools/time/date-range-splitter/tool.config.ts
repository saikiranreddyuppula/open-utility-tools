import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'time-date-range-splitter-v1',
  name: 'Date Range Splitter',
  slug: 'date-range-splitter',
  description: 'Split a date range into equal chunks or fixed-length intervals (e.g. weekly buckets).',
  category: 'time',
  tags: ['date', 'range', 'split', 'buckets', 'intervals'],
  keywords: ['date range split', 'date buckets', 'weekly buckets', 'chunk dates', 'reporting periods', 'date intervals'],
  icon: 'SplitSquareHorizontal',
  relatedTools: [],
};

export default meta;
