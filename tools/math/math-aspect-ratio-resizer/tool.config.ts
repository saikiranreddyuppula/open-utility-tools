import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'math-math-aspect-ratio-resizer-v1',
  name: 'Dimension Aspect-Ratio Solver',
  slug: 'math-aspect-ratio-resizer',
  description: 'Solve a missing width or height that preserves a given aspect ratio.',
  category: 'math',
  tags: ['aspect-ratio', 'resize', 'dimensions', 'scaling', 'pixels'],
  keywords: ['aspect ratio', 'resize', 'proportional', 'width', 'height', 'scale factor', 'gcd'],
  icon: 'Scaling',
  relatedTools: [],
};

export default meta;
