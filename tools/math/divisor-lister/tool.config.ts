import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'math-divisor-lister-v1',
  name: 'Divisor Lister & Counter',
  slug: 'divisor-lister',
  description: 'List every divisor of a number with totals, divisor count, and divisor pairs.',
  category: 'math',
  tags: ['divisors', 'factors', 'number-theory', 'sigma', 'aliquot'],
  keywords: ['factor list', 'divisor count', 'divisor sum', 'proper divisors', 'perfect number'],
  icon: 'List',
  relatedTools: [],
};

export default meta;
