import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'math-math-linear-system-solver-v1',
  name: 'Linear System Solver (Ax = b)',
  slug: 'math-linear-system-solver',
  description: 'Solves a system of linear equations using Gaussian elimination and reports the solution.',
  category: 'math',
  tags: ['linear-algebra', 'gaussian-elimination', 'matrix', 'equations', 'solver'],
  keywords: ['linear system', 'gaussian elimination', 'simultaneous equations', 'matrix solve', 'Ax=b', 'pivoting'],
  icon: 'SquareFunction',
  relatedTools: [],
};

export default meta;
