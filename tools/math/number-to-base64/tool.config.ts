import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'math-number-to-base64-v1',
  name: 'Number to Base64 Encoder',
  slug: 'number-to-base64',
  description: 'Encode an integer as compact Base64 bytes and decode Base64 back to a number.',
  category: 'math',
  tags: ['base64', 'integer', 'encoding', 'bytes', 'token'],
  keywords: ['number to base64', 'big integer encode', 'url-safe base64', 'byte order', 'compact id'],
  icon: 'Binary',
  relatedTools: [],
};

export default meta;
