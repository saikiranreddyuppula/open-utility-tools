import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'color-color-scale-generator-v1',
  name: 'Color Scale Generator',
  slug: 'color-scale-generator',
  description:
    'Generate an evenly stepped color scale between two colors, choosing the number of steps and interpolation space.',
  category: 'color',
  tags: ['scale', 'interpolate', 'ramp'],
  keywords: ['color scale', 'steps', 'interpolate', 'ramp', 'between', 'gradient stops'],
  icon: 'Spline',
  relatedTools: [],
};

export default meta;
