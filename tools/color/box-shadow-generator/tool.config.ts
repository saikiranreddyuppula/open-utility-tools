import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'color-box-shadow-generator-v1',
  name: 'CSS Box Shadow Generator',
  slug: 'box-shadow-generator',
  description:
    'Visually design CSS box-shadows by adjusting offset, blur, spread, color, and inset, then copy the ready-to-use box-shadow rule.',
  category: 'color',
  tags: ['box shadow', 'css', 'shadow', 'generator'],
  keywords: ['box shadow', 'css', 'shadow', 'blur', 'spread', 'generator'],
  icon: 'Layers',
  relatedTools: [],
};

export default meta;
