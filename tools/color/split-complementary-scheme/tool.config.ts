import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'color-split-complementary-scheme-v1',
  name: 'Split-Complementary Scheme Generator',
  slug: 'split-complementary-scheme',
  description: 'Generate a base plus the two colors adjacent to its complement.',
  category: 'color',
  tags: ['color', 'scheme', 'complementary', 'harmony', 'palette'],
  keywords: ['split complementary', 'color wheel', 'complement', 'accent', 'harmony', 'hsl'],
  icon: 'GitCompare',
  relatedTools: [],
};

export default meta;
