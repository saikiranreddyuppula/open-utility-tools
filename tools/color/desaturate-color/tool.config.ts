import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'color-desaturate-color-v1',
  name: 'Desaturate Color',
  slug: 'desaturate-color',
  description: "Reduce a color's saturation by a percentage and preview the muted result.",
  category: 'color',
  tags: ['color', 'desaturate', 'saturation', 'muted', 'filter'],
  keywords: ['reduce saturation', 'mute color', 'grayscale partial', 'css saturate filter', 'tone down color'],
  icon: 'Droplet',
  relatedTools: [],
};

export default meta;
