import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'color-color-name-finder-v1',
  name: 'Color Name Finder',
  slug: 'color-name-finder',
  description:
    "Find the closest human-readable color name (like 'cornflower blue') for any HEX or RGB value using the CSS named color set.",
  category: 'color',
  tags: ['color name', 'css colors', 'nearest', 'identify'],
  keywords: ['color name', 'nearest', 'css colors', 'what color', 'identify'],
  icon: 'Pipette',
  relatedTools: [],
};

export default meta;
