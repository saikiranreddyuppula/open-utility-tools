import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'web-web-multi-column-generator-v1',
  name: 'CSS Multi-Column Layout Generator',
  slug: 'web-multi-column-generator',
  description:
    'Generate CSS columns properties (count, width, gap, rule) with live text preview.',
  category: 'web',
  tags: ['css', 'columns', 'layout', 'multicol'],
  keywords: [
    'css columns',
    'multi-column',
    'column-count',
    'column-gap',
    'column-rule',
    'column-fill',
    'layout',
  ],
  icon: 'AlignJustify',
  relatedTools: [],
};

export default meta;
