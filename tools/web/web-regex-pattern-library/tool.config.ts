import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'web-web-regex-pattern-library-v1',
  name: 'Common Regex Patterns Library',
  slug: 'web-regex-pattern-library',
  description:
    'Searchable reference of ready-to-copy regex patterns for emails, URLs, IPs, dates, and other common validations.',
  category: 'web',
  tags: ['regex', 'patterns', 'reference', 'validation', 'cheatsheet'],
  keywords: [
    'regular expression',
    'regex library',
    'email regex',
    'url regex',
    'ipv4',
    'uuid',
    'validation pattern',
    'common regex',
  ],
  icon: 'BookOpen',
  relatedTools: [],
};

export default meta;
