import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'web-web-url-query-sorter-v1',
  name: 'URL Query Param Sorter',
  slug: 'web-url-query-sorter',
  description: 'Canonicalize a URL by sorting and optionally deduplicating query parameters.',
  category: 'web',
  tags: ['url', 'query', 'sort', 'canonical', 'params'],
  keywords: ['querystring', 'normalize', 'dedupe', 'search params', 'canonicalize url', 'order'],
  icon: 'ArrowDownUp',
  relatedTools: [],
};

export default meta;
