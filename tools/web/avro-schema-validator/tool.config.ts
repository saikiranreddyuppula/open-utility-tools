import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: "web-avro-schema-validator-v1",
  name: "Avro Schema Validator",
  slug: "avro-schema-validator",
  description: "Validate common Avro schema structure and summarize records, fields, enums, and unions.",
  category: "web",
  tags: ["avro","schema","json","events"],
  keywords: ["avro","schema","json","events"],
  icon: "FileJson",
  relatedTools: [],
};

export default meta;
