import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: "web-asyncapi-validator-preview-v1",
  name: "AsyncAPI Validator Preview",
  slug: "asyncapi-validator-preview",
  description: "Validate common AsyncAPI structure and summarize channels, operations, messages, and servers.",
  category: "web",
  tags: ["asyncapi","events","api","yaml"],
  keywords: ["asyncapi","events","api","yaml"],
  icon: "Radio",
  relatedTools: [],
};

export default meta;
