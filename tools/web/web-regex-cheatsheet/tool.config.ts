import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'web-web-regex-cheatsheet-v1',
  name: 'Regex Cheatsheet & Builder',
  slug: 'web-regex-cheatsheet',
  description:
    'Browse a searchable regex syntax reference and snippet library for common patterns like email, URL, IP, and date, with copyable expressions.',
  category: 'web',
  tags: ['regex', 'cheatsheet', 'reference'],
  keywords: ['regex', 'cheatsheet', 'reference', 'pattern', 'snippet', 'syntax'],
  icon: 'Regex',
  relatedTools: [],
};

export default meta;
