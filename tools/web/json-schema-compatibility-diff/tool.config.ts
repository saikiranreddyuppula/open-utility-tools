import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: "web-json-schema-compatibility-diff-v1",
  name: "JSON Schema Compatibility Diff",
  slug: "json-schema-compatibility-diff",
  description: "Compare JSON Schemas for required fields, property changes, and enum narrowing.",
  category: "web",
  tags: ["json schema","schema","compatibility","diff"],
  keywords: ["json schema","schema","compatibility","diff"],
  icon: "GitCompare",
  relatedTools: [],
};

export default meta;
