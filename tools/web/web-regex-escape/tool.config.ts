import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'web-web-regex-escape-v1',
  name: 'Regex Escape / Unescape',
  slug: 'web-regex-escape',
  description: 'Escape arbitrary text into a literal regex pattern or unescape an escaped pattern back to plain text.',
  category: 'web',
  tags: ['regex', 'escape', 'pattern', 'literal', 'text'],
  keywords: ['regex escape', 'regexp escape', 'literal pattern', 'escape special characters', 'unescape regex', 'metacharacters'],
  icon: 'Regex',
  relatedTools: [],
};

export default meta;
