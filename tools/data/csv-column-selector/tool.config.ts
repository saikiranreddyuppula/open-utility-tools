import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'data-csv-column-selector-v1',
  name: 'CSV Column Selector',
  slug: 'csv-column-selector',
  description: 'Keep, drop, or reorder CSV columns by header name or index.',
  category: 'data',
  tags: ['csv', 'columns'],
  keywords: ['csv', 'columns', 'select', 'reorder', 'filter'],
  icon: 'Filter',
  relatedTools: [],
};

export default meta;
