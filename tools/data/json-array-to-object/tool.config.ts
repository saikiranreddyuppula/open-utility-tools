import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'data-json-array-to-object-v1',
  name: 'JSON Array to Object',
  slug: 'json-array-to-object',
  description: 'Index a JSON array of objects into a keyed object/map.',
  category: 'data',
  tags: ['json', 'array', 'object', 'index', 'map'],
  keywords: ['json', 'array', 'object', 'index by', 'keyby', 'lookup', 'group by', 'projection'],
  icon: 'Boxes',
  relatedTools: [],
};

export default meta;
