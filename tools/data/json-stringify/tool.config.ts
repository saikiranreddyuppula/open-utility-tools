import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'data-json-stringify-v1',
  name: 'JSON Stringify / Parse',
  slug: 'json-stringify',
  description: 'Turn raw text into an escaped JSON string literal, or parse a JSON string back to its raw value.',
  category: 'data',
  tags: ['json', 'string', 'escape'],
  keywords: ['json', 'stringify', 'parse', 'escape', 'string'],
  icon: 'Quote',
  relatedTools: [],
};

export default meta;
