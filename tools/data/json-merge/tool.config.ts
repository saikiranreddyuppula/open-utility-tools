import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'data-json-merge-v1',
  name: 'JSON Merge',
  slug: 'json-merge',
  description: 'Deep-merge two JSON objects, with options for how arrays and conflicting keys are combined.',
  category: 'data',
  tags: ['json', 'merge'],
  keywords: ['json', 'merge', 'combine', 'deep merge', 'objects'],
  icon: 'Combine',
  relatedTools: [],
};

export default meta;
