import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'data-json-to-query-string-v1',
  name: 'JSON to Query String',
  slug: 'json-to-query-string',
  description: 'Convert a flat JSON object into a URL query string, with array and encoding options.',
  category: 'data',
  tags: ['json', 'url'],
  keywords: ['json', 'query string', 'url', 'params', 'convert'],
  icon: 'Link2',
  relatedTools: [],
};

export default meta;
