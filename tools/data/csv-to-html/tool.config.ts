import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'data-csv-to-html-v1',
  name: 'CSV to HTML Table',
  slug: 'csv-to-html',
  description: 'Convert CSV into a clean HTML table element with a header row, ready to paste into a page.',
  category: 'data',
  tags: ['csv', 'html'],
  keywords: ['csv', 'html', 'table', 'convert', 'export'],
  icon: 'Table',
  relatedTools: [],
};

export default meta;
