import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'data-json-to-graphql-sdl-v1',
  name: 'JSON to GraphQL Type',
  slug: 'json-to-graphql-sdl',
  description: 'Generate GraphQL SDL type definitions from a JSON sample.',
  category: 'data',
  tags: ['json', 'graphql', 'sdl', 'schema', 'codegen'],
  keywords: ['json to graphql', 'sdl', 'graphql type', 'schema definition', 'codegen'],
  icon: 'Braces',
  relatedTools: [],
};

export default meta;
