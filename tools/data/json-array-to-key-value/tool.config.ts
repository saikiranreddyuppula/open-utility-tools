import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'data-json-array-to-key-value-v1',
  name: 'JSON Array to Lookup Map',
  slug: 'json-array-to-key-value',
  description: 'Convert a JSON array of objects into a keyed lookup object using one field as the key.',
  category: 'data',
  tags: ['json', 'array', 'lookup', 'map', 'index'],
  keywords: ['json', 'array', 'lookup', 'map', 'keyby', 'index by', 'dictionary', 'object'],
  icon: 'ListTree',
  relatedTools: [],
};

export default meta;
