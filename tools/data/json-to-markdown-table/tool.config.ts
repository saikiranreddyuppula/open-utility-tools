import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'data-json-to-markdown-table-v1',
  name: 'JSON to Markdown Table',
  slug: 'json-to-markdown-table',
  description: 'Render an array of JSON objects as a GitHub-flavored Markdown table.',
  category: 'data',
  tags: ['json', 'markdown', 'table'],
  keywords: ['json', 'markdown', 'table', 'convert', 'github'],
  icon: 'FileText',
  relatedTools: [],
};

export default meta;
