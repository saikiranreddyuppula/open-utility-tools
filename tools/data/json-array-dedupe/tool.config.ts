import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'data-json-array-dedupe-v1',
  name: 'JSON Array Deduplicate',
  slug: 'json-array-dedupe',
  description: 'Remove duplicate elements from a JSON array, optionally by key.',
  category: 'data',
  tags: ['json', 'array', 'dedupe', 'unique', 'duplicates'],
  keywords: ['json', 'array', 'dedupe', 'deduplicate', 'unique', 'distinct', 'remove duplicates', 'by key'],
  icon: 'Filter',
  relatedTools: [],
};

export default meta;
