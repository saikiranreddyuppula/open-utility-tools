import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'data-json-to-dart-v1',
  name: 'JSON to Dart Class',
  slug: 'json-to-dart',
  description: 'Generate Dart classes with fromJson/toJson from a JSON sample.',
  category: 'data',
  tags: ['json', 'dart', 'flutter', 'codegen', 'class'],
  keywords: ['json to dart', 'flutter model', 'fromjson', 'tojson', 'data class', 'serialization'],
  icon: 'FileCode',
  relatedTools: [],
};

export default meta;
