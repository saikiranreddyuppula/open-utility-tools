import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'convert-json-array-to-list-v1',
  name: 'JSON Array to List',
  slug: 'json-array-to-list',
  description: 'Convert a JSON array into a delimited or line-separated list.',
  category: 'convert',
  tags: ['json', 'array', 'list', 'convert', 'delimiter'],
  keywords: ['json array to list', 'flatten array', 'join', 'extract key', 'csv list'],
  icon: 'ListOrdered',
  relatedTools: [],
};

export default meta;
