import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'convert-json-to-ini-v1',
  name: 'JSON to INI',
  slug: 'json-to-ini',
  description: 'Convert a JSON object into INI / config file syntax.',
  category: 'convert',
  tags: ['json', 'ini', 'config', 'convert', 'sections'],
  keywords: ['json to ini', 'ini config', 'sections', 'key value', 'settings file'],
  icon: 'FileCog',
  relatedTools: [],
};

export default meta;
