import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'convert-json-to-properties-v1',
  name: 'JSON to Java .properties',
  slug: 'json-to-properties',
  description: 'Convert nested JSON into Java/Spring .properties lines.',
  category: 'convert',
  tags: ['json', 'properties', 'java', 'spring', 'convert'],
  keywords: ['json to properties', 'java properties', 'spring config', 'application.properties', 'flatten'],
  icon: 'FileText',
  relatedTools: [],
};

export default meta;
