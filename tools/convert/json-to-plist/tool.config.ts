import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'convert-json-to-plist-v1',
  name: 'JSON to Apple plist',
  slug: 'json-to-plist',
  description: 'Convert JSON into an Apple XML property list.',
  category: 'convert',
  tags: ['json', 'plist', 'apple', 'xml', 'convert'],
  keywords: ['json to plist', 'property list', 'apple plist', 'macos', 'ios config'],
  icon: 'FileCode',
  relatedTools: [],
};

export default meta;
