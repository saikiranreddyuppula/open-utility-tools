import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'convert-plist-to-json-v1',
  name: 'Apple plist to JSON',
  slug: 'plist-to-json',
  description: 'Convert an Apple XML property list into JSON.',
  category: 'convert',
  tags: ['plist', 'json', 'apple', 'xml', 'convert'],
  keywords: ['plist to json', 'property list', 'apple plist', 'xml plist', 'macos', 'ios config'],
  icon: 'FileJson',
  relatedTools: [],
};

export default meta;
