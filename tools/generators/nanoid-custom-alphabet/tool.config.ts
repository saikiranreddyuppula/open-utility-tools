import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'generators-nanoid-custom-alphabet-v1',
  name: 'NanoID Custom Alphabet Generator',
  slug: 'nanoid-custom-alphabet',
  description: 'Generate NanoIDs from your own alphabet with an unbiased rejection-sampling algorithm.',
  category: 'generators',
  tags: ['nanoid', 'id', 'random', 'alphabet', 'unique'],
  keywords: ['nanoid', 'custom alphabet', 'unique id', 'short id', 'rejection sampling', 'url safe id'],
  icon: 'Type',
  relatedTools: [],
};

export default meta;
