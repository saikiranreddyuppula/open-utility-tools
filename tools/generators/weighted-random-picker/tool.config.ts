import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'generators-weighted-random-picker-v1',
  name: 'Weighted Random Picker',
  slug: 'weighted-random-picker',
  description: 'Pick random items from a weighted list using cumulative-weight sampling.',
  category: 'generators',
  tags: ['random', 'weighted', 'picker', 'sampling', 'lottery'],
  keywords: ['weighted random', 'random picker', 'weighted choice', 'probability', 'raffle', 'sampling'],
  icon: 'Scale',
  relatedTools: [],
};

export default meta;
