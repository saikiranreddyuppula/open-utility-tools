import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'generators-generators-svg-qr-style-placeholder-v1',
  name: 'Random Identicon (SVG)',
  slug: 'generators-svg-qr-style-placeholder',
  description: 'Generates a deterministic symmetric pixel identicon SVG from a seed string.',
  category: 'generators',
  tags: ['identicon', 'svg', 'avatar', 'placeholder', 'deterministic'],
  keywords: ['identicon', 'avatar', 'svg', 'gravatar', 'pixel art', 'seed', 'github avatar'],
  icon: 'Grid3x3',
  relatedTools: [],
};

export default meta;
