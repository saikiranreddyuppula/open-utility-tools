import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'generators-generators-json-server-routes-v1',
  name: 'REST Mock Routes Generator',
  slug: 'generators-json-server-routes',
  description: 'Generates a JSON config of CRUD REST routes from a list of resource names.',
  category: 'generators',
  tags: ['rest', 'api', 'routes', 'crud', 'openapi'],
  keywords: ['rest routes', 'crud', 'json-server', 'openapi', 'endpoints', 'mock api', 'resources'],
  icon: 'Route',
  relatedTools: [],
};

export default meta;
