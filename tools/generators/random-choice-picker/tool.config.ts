import type { ToolMetaStatic } from '@/lib/registry/types';

const meta: ToolMetaStatic = {
  id: 'generators-random-choice-picker-v1',
  name: 'Random Choice Picker',
  slug: 'random-choice-picker',
  description:
    'Pick one or several random options from a list of choices, with or without replacement.',
  category: 'generators',
  tags: ['random', 'picker', 'choice', 'shuffle', 'decision'],
  keywords: ['random picker', 'pick one', 'choose', 'raffle', 'decision maker', 'shuffle list'],
  icon: 'Pointer',
  relatedTools: [],
};

export default meta;
